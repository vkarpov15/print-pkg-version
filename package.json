{
  "name": "print-pkg-version",
  "version": "0.2.1",
  "devDependencies": {
    "standard-changelog": "1.0.0"
  },
  "bin": {
    "print-pkg-version": "./index.js"
  },
  "scripts": {
    "release": "standard-changelog && git commit -a -m 'chore: release `node .`' && git push origin master --tags && npm publish"
  }
}
